@using System.Globalization
@model ActorVM
<div class="col-xxl-12 col-xl-12">
    <div class="card" id="ticketsTable" data-list='{"valueNames":["client","subject","status","priority","agent"],"page":11,"pagination":true,"fallback":"tickets-table-fallback"}'>
        <div class="card-header border-bottom border-200 px-0">
            <div class="d-lg-flex justify-content-between">
                <div class="row flex-between-center gy-2 px-x1">
                    <div class="col-auto pe-0">
                        <h3 class="mb-0">Actors</h3>
                    </div>
                    <div class="col-auto">
                        <form>
                            <div class="input-group input-search-width">
                                <input class="form-control form-control-sm shadow-none search" type="search" placeholder="Search  by name" aria-label="search" name="actorName" value="@ViewBag.actorName" />
                                <button type="submit" class="btn btn-sm btn-outline-secondary border-300 hover-border-secondary"><span class="fa fa-search fs--1"></span></button>
                            </div>
                        </form>
                    </div>
                </div>
                <div class="border-bottom border-200 my-3"></div>
                <div class="d-flex align-items-center justify-content-between justify-content-lg-end px-x1">
                    <button class="btn btn-sm btn-falcon-default d-xl-none" type="button" data-bs-toggle="offcanvas" data-bs-target="#ticketOffcanvas" aria-controls="ticketOffcanvas"><span class="fas fa-filter" data-fa-transform="shrink-4 down-1"></span><span class="ms-1 d-none d-sm-inline-block">Filter</span></button>
                    <div class="bg-300 mx-3 d-none d-lg-block d-xl-none" style="width:1px; height:29px"></div>
                    <div class="d-none" id="table-ticket-actions">
                        <div class="d-flex">
                            <select class="form-select form-select-sm" aria-label="Bulk actions">
                                <option selected="">Bulk actions</option>
                                <option value="Refund">Refund</option>
                                <option value="Delete">Delete</option>
                                <option value="Archive">Archive</option>
                            </select>
                            <button class="btn btn-falcon-default btn-sm ms-2" type="button">Apply</button>
                        </div>
                    </div>
                    <div class="d-flex align-items-center" id="table-ticket-replace-element">

                        <a href="/Admin/Actor/Create" class="btn btn-falcon-success btn-md mx-5 px-4 py-2 fs-0">
                            <span class="fas fa-plus" data-fa-transform="shrink-3"></span>
                            <span class="d-none d-sm-inline-block d-xl-none d-xxl-inline-block ms-1">New</span>
                        </a>                    <div class="dropdown font-sans-serif ms-2">
                    </div>
                </div>
            </div>
        </div>

        @foreach (var item in Model.Actors)
        {
            <div class="col-12">
                <div class="card font-sans-serif">
                    <div class="card-body d-flex gap-3 flex-column flex-sm-row align-items-center">
                        <img class="rounded-3" src="~/images/actor_images/@item.Img" alt="" width="112" />
                        <table class="table table-borderless fs--1 fw-medium mb-0">
                            <tbody>
                                <tr>
                                    <td class="p-1" style="width: 35%;">Name :</td>
                                    <td class="p-1 text-600">@item.Name</td>
                                </tr>
                                <tr>
                                    <td class="p-1" style="width: 35%;">Gender :</td>
                                    <td class="p-1 text-600">@item.Gender</td>
                                </tr>
                                <tr>
                                    <td class="p-1" style="width: 35%;">Rate :</td>

                                    <td class="p-1" style="width: 35%;">
                                        @{
                                            int fullStars = (int)Math.Floor(item.Rate);
                                            bool halfStar = item.Rate % 1 >= 0.5m;
                                        }

                                        @for (int i = 0; i < fullStars; i++)
                                        {
                                            <span class="fas fa-star text-warning"></span>
                                        }

                                        @if (halfStar)
                                        {
                                            <span class="fas fa-star-half-alt text-warning"></span>
                                        }

                                        @for (int i = fullStars + (halfStar ? 1 : 0); i < 5; i++)
                                        {
                                            <span class="far fa-star text-muted"></span>
                                        }

                                        <span class="ms-2">@item.Rate.ToString("0.0")/5</span>
                                    </td>

                                    <td class="p-1" style="width: 35%;">
                                        <a href="/Admin/Actor/Edit/@item.Id" class="btn btn-falcon-default btn-sm" type="button"><span class="fas fa-external-link-alt" data-fa-transform="shrink-3"></span><span class="d-none d-sm-inline-block d-xl-none d-xxl-inline-block ms-1">Export</span></a>
                                        <a href="/Admin/Actor/Delete/@item.Id" class="btn btn-falcon-danger btn-sm" type="button">
                                            <span class="fas fa-trash" data-fa-transform="shrink-3"></span>
                                            <span class="d-none d-sm-inline-block d-xl-none d-xxl-inline-block ms-1">Delete</span>
                                        </a>
                                    </td>

                                </tr>

                            </tbody>
                        </table>

                    </div>
                </div>
            </div>
        }
        <div class="card-footer">
            <div class="d-flex justify-content-center align-items-center gap-1">

                @* Previous *@
                <a asp-action="Index" asp-route-page="@(Model.CurrentPage - 1)"
                   class="btn btn-sm btn-falcon-default @(Model.CurrentPage == 1 ? "disabled" : "")">
                    <span class="fas fa-chevron-left"></span>
                </a>

                @* Page Numbers *@
                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    <a asp-action="Index" asp-route-page="@i"
                       class="btn btn-sm @(i == Model.CurrentPage ? "btn-primary" : "btn-falcon-default")">
                        @i
                    </a>
                }

                @* Next *@
                <a asp-action="Index" asp-route-page="@(Model.CurrentPage + 1)"
                   class="btn btn-sm btn-falcon-default @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
                    <span class="fas fa-chevron-right"></span>
                </a>

            </div>
        </div>
    </div>
</div>
